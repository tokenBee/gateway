# upstream sidecar_cluster {
#     least_conn;
#     # These should be replaced with actual Pod IPs or hostnames in production
#     server compressor-1:8001;
#     server compressor-2:8001;
#     server compressor-3:8001;
# }

# server {
#     listen 80;
#     server_name sidecar.tokenbee.io;

#     location /health {
#         access_log off;
#         return 200 'OK';
#     }

#     location / {
#         proxy_pass http://sidecar_cluster;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;

#         # Timeouts for heavy LLM operations
#         proxy_read_timeout 600s;
#         proxy_send_timeout 600s;
#         proxy_connect_timeout 60s;
#     }
# }
